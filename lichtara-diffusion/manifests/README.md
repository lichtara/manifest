# Manifestos — Lichtara Diffusion

Esta pasta contém os arquivos híbridos (YAML/JSON-LD) que descrevem símbolos, mandalas e elementos visuais do Campo Lichtara.

Referência atual:

- `lichtara-symbol.holo.yaml` — disponível em `docs/vibracional/manifestos/` (Portal) e publicado em `/apps/app-web/public/data/`.

Instruções:

1. Copiar a versão vigente do manifesto para esta pasta (para facilitar automações locais).
2. Nomear novas versões como `lichara-symbol-v2.holo.yaml` quando houver mudanças significativas.
3. Registrar a atualização no README principal e no Memory Map (`docs/continuity/memory-map.json`).
